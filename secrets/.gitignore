# Agenix secrets directory

# Keep encrypted secrets (.age files)
*.age

# Ignore decrypted secrets (these should never be committed)
*
!*.age
!.gitignore
!secrets.nix
!keys/
!README.md

# Keep the keys directory structure
keys/*
!keys/.gitignore
!keys/README.md
